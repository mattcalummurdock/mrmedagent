{
  "name": "med-cube",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/start-cube.js",
    "start": "node scripts/start-cube.js",
    "diagnostics": "node -e \"require('./lib/diagnostics').runStartupDiagnostics(require('path').join(__dirname)).then(r=>console.log(r))\""
  },
  "dependencies": {
    "@cubejs-backend/postgres-driver": "^1.3.0",
    "@cubejs-backend/server": "^1.3.0"
  }
}
